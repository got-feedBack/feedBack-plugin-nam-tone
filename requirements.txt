# Runtime deps beyond what the host app already provides.
# python-multipart: required by FastAPI for UploadFile/File form parsing
# (model/IR upload endpoints). Also installed by the org reusable CI.
python-multipart>=0.0.30
